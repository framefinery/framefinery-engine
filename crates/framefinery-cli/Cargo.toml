[package]
name = "framefinery"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Safe Rust media pipeline toolkit and FrameFinery command-line encoder."
documentation = "https://docs.rs/framefinery"
default-run = "ff"

[lib]
name = "framefinery"
path = "src/lib.rs"

[[bin]]
name = "ff"
path = "src/main.rs"

[dependencies]
framefinery-core = { path = "../framefinery-core", version = "0.0.1" }
framefinery-codecs = { path = "../framefinery-codecs", version = "0.0.1", optional = true, default-features = false }

[features]
default = ["all-codecs", "all-filters"]
all-codecs = ["av2", "vvc"]
all-filters = ["filter-pattern", "filter-identity", "filter-crop", "filter-scale"]
av2 = ["codec-av2"]
vvc = ["codec-vvc"]
codec-av2 = ["dep:framefinery-codecs", "framefinery-codecs/av2"]
codec-vvc = ["dep:framefinery-codecs", "framefinery-codecs/vvc"]
filter-pattern = []
filter-identity = []
filter-crop = []
filter-scale = []

[lints]
workspace = true

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]
